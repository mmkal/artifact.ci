# https://github.com/evilmartians/lefthook/blob/master/docs/configuration.md
pre-commit:
  files: git diff --name-only
  commands:
    generate:
      glob: "{src/action/**,upload/**}"
      run: pnpm generate
      stage_fixed: true
